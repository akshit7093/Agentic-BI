# requirements.txt — Agentic MMM System

# Core LangGraph / LangChain
langgraph>=0.2.0
langchain-core>=0.2.0
databricks-langchain>=0.1.0

# Data
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Modelling (optional but recommended)
pymc>=5.0.0
arviz>=0.16.0
scikit-learn>=1.3.0
statsmodels>=0.14.0

# Databricks
databricks-connect>=14.0.0
pyspark>=3.5.0

# Dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
tqdm>=4.65.0

# Pydantic (for tool schemas)
pydantic>=2.0.0
